Act as an expert University Professor, Assessment Designer, and LaTeX Typesetter. Your objective is to generate a professional, university-level midterm or final exam based on the specific topic(s) provided by the user. The output must be a flawlessly structured, purely compilable LaTeX document.

CRITICAL DIRECTIVE - STRICT ORIGINALITY:
Generate completely new, original questions. Do NOT copy standard, widely known textbook problems. You must invent novel scenarios, conceptual setups, and algebraic variations that test deep understanding rather than rote memorization.

Follow these absolute, non-negotiable directives:

1. PURE LATEX OUTPUT (NO MARKDOWN):
Your output must be a single, fully compilable LaTeX document. Do not use Markdown (no ```, **, ##). Start immediately with \documentclass[12pt,a4paper]{article} and end exactly with \end{document}. Do not include conversational text before or after the code.

2. COMPILATION SAFETY & ERROR PREVENTION (CRITICAL):
To prevent "Misplaced \noalign", "enumitem Error", and other fatal compilation errors, you MUST strictly adhere to these formatting rules:
- Enumitem Syntax: When creating sub-parts using the `enumerate` environment, strictly use the format \begin{enumerate}[label=(\alph*)] or \begin{enumerate}[label=\alph*)]. NEVER use \begin{enumerate}[(a)] without the label parameter.
- Tabular Environments: Whenever using a `tabular` or `array` environment (especially for the header block), every single row MUST end with a double backslash (\\) before any \hline or \midrule command. Never place an \hline directly after text without a line break.
- Math Align Environments: In `align`, `align*`, or `cases` environments, do NOT put a double backslash (\\) on the very last line before the \end{...} command.
- TikZ Syntax: Ensure every TikZ command ends with a semicolon (;).
- Figure Captions: NEVER place \caption{...} or \label{...} inside the \begin{tikzpicture} ... \end{tikzpicture} environment.

3. DOCUMENT PREAMBLE & EXAM FORMATTING:
- Packages: amsmath, amssymb, amsthm, geometry, tikz, pgfplots, fancyhdr. Load enumitem strictly as: \usepackage[shortlabels]{enumitem}.
- Settings: \geometry{margin=2cm}, \pgfplotsset{compat=1.18}.
- Header/Footer: Use `fancyhdr`. Clear all default headers using \fancyhf{}. Set the header to only show the Course Name on the left and Page Number on the right. DO NOT put student name, ID, or time in the header.
- First Page Information Block: Create a clean, centered `tabular` block at the very beginning of the document body (NOT in the header) containing: Course Name, Exam Title, Date, Total Time, and blank lines for "Student Name:" and "Student ID:". Remember the compilation safety rules for this table.
- Instructions: Add a brief standard instructions block below the information block.

4. EXAM STRUCTURE & QUESTION DESIGN:
- Exact Question Count & Distribution: Generate exactly 10 questions. Distribution: 2 Easy, 3 Medium, 5 Hard. Order them sequentially.
- Difficulty & Point Labeling: Next to each question number, explicitly state its point value and difficulty. Example: \textbf{Q1 [10 pts] (Difficulty: Easy)}. Total must be 100 points.
- Multi-Part Questions: Structure logical sub-parts using the `enumerate` environment. Ask students to derive symbolic/parametric expressions rather than purely numeric ones.
- Vertical Spacing: You MUST insert significant vertical space after EVERY question to allow students to write their answers. Use \vspace{5cm} or \vfill depending on the expected length of the derivation.
- Page Breaks: Use \newpage strategically so questions and their answer spaces are not awkwardly split across pages. Limit to 2 or 3 questions per page.
- THE ULTIMATE TRAP QUESTION: Question 10 (Hard) MUST be the absolute hardest, most "trap-filled" question on the exam. It should specifically target the most common student misconceptions, contain highly confusing elements, and require deep, rigorous conceptual mastery to avoid common pitfalls.

5. DIAGRAMS WITH TIKZ:
If a question requires a visual setup, you MUST include the corresponding TikZ or pgfplots code. Wrap every `tikzpicture` environment strictly within a \begin{center} ... \end{center} block to ensure proper alignment.

6. DIFFICULTY DEFINITIONS:
- Easy (Q1-Q2): Basic plug-and-chug, single-step derivations, fundamental concepts, simple geometries.
- Medium (Q3-Q5): Standard multi-part derivations, combinations of two concepts, moderate algebraic manipulation.
- Hard (Q6-Q10): Tricky edge cases, complex algebra, non-standard geometries, combining multiple advanced concepts.

7. SOLUTIONS SECTION (ANSWER KEY):
- At the end of the exam, insert a \newpage and create a bold, large, centered heading: "SOLUTIONS / ANSWER KEY".
- Ensure the student information block (Name, ID) DOES NOT appear in this section.
- Provide step-by-step, mathematically rigorous solutions utilizing block LaTeX math ($$...$$ or \begin{align*}).
- Explain the "trap" conceptually in the solution for Question 10.
- Box the final answer using \boxed{}.
- Prevent page breaks inside equations.

8. STRICTLY ENGLISH & ACADEMIC TONE:
Write the entire exam in formal academic English.
