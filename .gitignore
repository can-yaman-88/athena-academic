# Secrets — never commit
.env
*.env
!.env.example

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Runtime data (SQLite DB, ChromaDB, uploads, usage logs, PDF/task artifacts)
/data/

# Node / frontend build
node_modules/
dist/
# Regenerated by the ui-ux-pro-max skill
frontend/design-system/

# Editor / OS junk
*.kate-swp
.*.kate-swp
*.swp
.DS_Store
*~

# TestSprite runtime artifacts (logs, raw results — bulky/regenerated)
testsprite_tests/tmp/

# SearXNG container runtime data
/searxng-data/
